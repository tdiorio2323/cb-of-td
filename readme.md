### Route Gallery

To generate an HTML gallery previewing all application routes at mobile and desktop widths:

```bash
pnpm routes:gallery
# or
npm run routes:gallery
```

This will create `route-gallery/gallery.html` in the project root.